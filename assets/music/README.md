# assets/music/

Battle music `.mp3` files live here. Each file is registered as a row in
`music.json` (id, title, category, file, enabled) and is uploaded through the
data editor's Music tab, not committed by hand.
